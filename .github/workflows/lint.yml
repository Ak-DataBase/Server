name: Lint

on:
  push:
    branches:
      - dom
      - sub
  pull_request:
    branches:
      - dom
      - sub

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Run lint
      - uses: actions/checkout@v2
      - run: cargo lint
    
name: Build

on:
  push:
    branches:
      - dom
      - sub
  pull_request:
    branches:
      - dom
      - sub

jobs:
  lint:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Run build
      - uses: actions/checkout@v2
      - run: cargo build
    